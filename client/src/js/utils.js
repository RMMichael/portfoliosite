
const rootPath = '/';  // TODO don't hardcode path
const base = window.location.protocol + '//' + window.location.host + rootPath;
const apiBase = base + '/api/';


export { rootPath, base, apiBase };
